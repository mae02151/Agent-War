# Server Validation Spec V1

## 1. 목적

이 문서는 Agent Arena V1에서 서버가 action을 검증하는 규칙을 정의한다.

목표:
- 외부 에이전트가 제출한 action을 일관되게 판정
- 불법 행동, 잘못된 JSON, 시야 밖 정보 사용을 제어
- 매치를 중단하지 않고 계속 진행할 수 있게 설계

핵심 원칙:
- 서버가 authoritative 하다
- 클라이언트와 에이전트는 항상 신뢰하지 않는다
- 일부 action이 잘못되어도 전체 턴을 무조건 실패시키지 않는다

## 2. 검증 레이어

V1 검증은 아래 4단계로 나눈다.

1. 메시지 레벨 검증
2. 스키마 레벨 검증
3. 게임 상태 레벨 검증
4. 규칙 적용 및 부분 반영

## 3. 메시지 레벨 검증

### 3.1 필수 확인 항목
- `match_id` 존재 여부
- `turn` 존재 여부
- 현재 활성 경기와 일치하는지
- 현재 턴과 일치하는지
- 요청 플레이어가 올바른 주체인지

### 3.2 거부 조건
- 잘못된 `match_id`
- 이미 종료된 경기
- 과거 턴 또는 미래 턴 action 제출
- 다른 플레이어 슬롯의 행동 제출

처리:
- 해당 action payload 전체 거부
- `turn.rejected` 또는 서버 로그 기록

## 4. 스키마 레벨 검증

### 4.1 공통 규칙
- JSON 파싱 가능해야 한다
- 필수 필드가 있어야 한다
- 타입이 맞아야 한다
- 배열/문자열/정수 형식이 맞아야 한다

### 4.2 action 배열 규칙
- `actions`는 배열이어야 한다
- 각 action은 `type` 필드를 가져야 한다
- 알 수 없는 `type`은 무효

### 4.3 스키마 오류 처리
- action 하나가 스키마 오류면 해당 action만 무효 처리
- 전체 payload 자체가 파싱 불가면 전체 턴 거부

## 5. 게임 상태 레벨 검증

이 단계에서는 현재 서버 상태와 action의 내용이 실제로 가능한지 본다.

검증 항목:
- 대상 유닛/도시가 실제로 존재하는지
- 해당 유닛/도시가 현재 플레이어 소유인지
- 행동 시점에 행동 가능 상태인지
- 자원이 충분한지
- 위치가 유효한지
- 이동/공격/건설 조건을 만족하는지

## 6. Action 공통 검증 규칙

모든 action에 공통으로 적용한다.

- `unit_id`가 있으면 해당 유닛이 내 소유여야 한다
- `city_id`가 있으면 해당 도시가 내 소유여야 한다
- 이미 파괴된 대상은 참조할 수 없다
- 한 턴에 이미 소모된 유닛은 추가 행동 불가
- 이동력/사거리/자원 제약을 초과할 수 없다

## 7. Action 타입별 검증

## 7.1 `move`

검증:
- `unit_id` 존재 여부
- 목적지 좌표 유효 여부
- 목적지가 맵 안인지
- 통과 가능한 지형인지
- 현재 이동력으로 도달 가능한지
- 아군/적군 점유 규칙 위반이 없는지

무효 예시:
- 산으로 이동
- 이동력 초과
- 존재하지 않는 좌표

처리:
- 해당 `move`만 무효

## 7.2 `attack`

검증:
- 공격 유닛 존재 여부
- 공격 유닛이 공격 가능한 상태인지
- 목표 대상 존재 여부
- 목표가 현재 시야나 규칙상 공격 가능한 정보인지
- 목표가 사거리 내인지
- 아군 대상 공격이 아닌지

무효 예시:
- 시야 밖 적을 직접 target_id로 지정
- 사거리 밖 공격
- 이미 제거된 적 공격

처리:
- 해당 `attack`만 무효

## 7.3 `produce`

검증:
- `city_id` 존재 여부
- 생산 도시가 내 소유인지
- 생산 슬롯이 비어 있는지
- 생산 타입이 허용되는지
- 자원이 충분한지

무효 예시:
- 자원 부족
- 동시에 중복 생산
- 없는 유닛 타입 생산

처리:
- 해당 `produce`만 무효

## 7.4 `build`

검증:
- worker 유닛인지
- 목표 좌표가 유효한지
- build_type이 허용되는지
- 현재 타일 또는 인접 타일 조건을 만족하는지
- 이미 개발된 타일인지 여부

무효 예시:
- worker가 아닌 유닛이 build 시도
- 바다 타일에 잘못된 개발 시도

처리:
- 해당 `build`만 무효

## 7.5 `gather`

검증:
- gather 행동이 현재 규칙에서 활성인지
- 대상 좌표가 수집 가능한 자원 타일인지
- 해당 유닛이 수집 가능한 유닛인지

무효 예시:
- 자원이 없는 타일 수집
- worker가 아닌 유닛 수집

처리:
- 해당 `gather`만 무효

## 7.6 `capture`

검증:
- 유닛이 점령 가능한 타입인지
- 대상 타일 또는 거점이 점령 가능한 상태인지
- 점령 조건을 만족하는지
- 적 유닛이 같은 위치를 점유하고 있지 않은지

무효 예시:
- 점령 불가 타일
- 전투 중인 지역 점령 시도

처리:
- 해당 `capture`만 무효

## 7.7 `fortify`

검증:
- 유닛이 존재하는지
- 현재 위치에서 방어 태세가 가능한지
- 이미 행동을 소모했는지 여부

처리:
- 조건을 만족하면 fortify 상태 적용

## 7.8 `settle_city`

검증:
- settler 유닛인지
- 현재 타일이 도시 건설 가능한지
- 너무 가까운 기존 도시가 없는지
- 금지 지형이 아닌지
- 자원이 충분한지

무효 예시:
- 도시 간 거리 제한 위반
- 바다/산 타일 위 건설
- settler가 아닌 유닛 사용

처리:
- 해당 `settle_city`만 무효

## 7.9 `end_turn`

검증:
- 별도 조건 없이 허용

원칙:
- `end_turn`은 항상 유효
- 다른 action이 없어도 턴 종료 가능

## 8. 시야 검증 규칙

Fog of War 게임이므로 시야 검증이 중요하다.

### 8.1 기본 원칙
- 서버는 플레이어 실제 시야를 기준으로만 판정한다
- action이 시야 밖 정보를 전제하면 무효 가능

### 8.2 허용되는 경우
- 마지막 관측 위치를 향한 이동
- 추정 위치 주변 탐색
- objective order에 따른 지역 접근

### 8.3 허용되지 않는 경우
- 현재 보이지 않는 적 유닛 ID를 정확하게 직접 공격
- 존재를 알 수 없는 숨은 타일 상태를 전제로 행동

## 9. 자원 검증 규칙

### 9.1 생산 비용
- 자원 부족 시 생산 무효

### 9.2 동시 action 충돌
같은 턴에 여러 action이 자원을 같이 소비할 경우:

- 서버는 action 배열 순서대로 처리
- 앞 action이 자원을 소모하면 뒤 action은 자원 부족으로 무효 가능

예:
1. warrior 생산
2. archer 생산

첫 action 후 자원이 부족하면 두 번째는 무효

## 10. 유닛 상태 검증 규칙

### 10.1 한 턴 내 중복 사용
- 한 유닛이 이미 행동을 끝냈으면 추가 action 불가

### 10.2 이동 후 공격
- 유닛별 허용 여부를 규칙 테이블로 관리
- V1 기본안:
  - warrior: 이동 후 공격 가능
  - archer: 이동 후 공격 가능하되 밸런스에 따라 조정 가능
  - worker: 공격 불가
  - settler: 공격 불가

### 10.3 파괴 직후 참조
- 같은 턴 내 먼저 처리된 action으로 유닛이 제거되면, 뒤의 action에서 해당 유닛 참조는 무효

## 11. 부분 반영 원칙

V1 핵심 원칙:
- action 배열 전체를 한 번에 폐기하지 않는다
- 가능한 action은 최대한 반영한다

예:
1. `move(unit_1)`
2. `attack(unit_1, enemy_3)`
3. `produce(city_1, warrior)`

여기서 2번만 무효면:
- 1번 반영
- 2번 무효
- 3번 반영

## 12. 전체 턴 거부 조건

아래 상황은 payload 전체를 거부할 수 있다.

- JSON 자체 파싱 실패
- `match_id`, `turn` 누락
- 완전히 다른 턴에 대한 응답
- 인증 실패 또는 플레이어 불일치

이 경우:
- 서버는 fallback 정책을 사용
- 기본적으로 `end_turn` 처리 가능

## 13. Fallback 정책

### 13.1 timeout
- 응답이 없으면 자동 `end_turn`

### 13.2 전체 payload 거부
- 서버는 해당 턴을 무행동 또는 `end_turn`로 처리

### 13.3 연속 실패
- 연속 timeout 또는 전부 무효 payload가 누적되면 패널티 가능
- V1에서는 즉시 패배보다 로그 기록과 경고 우선

## 14. 서버 로그 정책

서버는 아래를 기록해야 한다.

- 제출 원본 action payload
- action별 검증 결과
- 무효 사유
- 최종 반영된 action 목록
- fallback 발생 여부

이 기록은 디버깅과 리플레이 분석에 중요하다.

## 15. 검증 결과 구조 제안

서버 내부에서는 action별 결과를 아래처럼 남기는 것이 좋다.

```json
{
  "index": 1,
  "action_type": "attack",
  "status": "rejected",
  "reason": "target_out_of_vision"
}
```

추천 상태값:
- `accepted`
- `rejected`
- `partially_applied`
- `skipped`

## 16. 추천 에러 코드

- `invalid_json`
- `invalid_schema`
- `invalid_match`
- `invalid_turn`
- `not_owner`
- `entity_not_found`
- `insufficient_resources`
- `out_of_range`
- `out_of_vision`
- `movement_exceeded`
- `invalid_target`
- `occupied_tile`
- `terrain_blocked`
- `action_already_spent`

## 17. V1 구현 우선순위

먼저 구현할 검증:

1. match/turn 검증
2. action schema 검증
3. ownership 검증
4. movement 검증
5. attack range / vision 검증
6. resource 검증
7. settle_city 거리 검증

그 다음 붙일 것:
- 세부 건설 규칙
- 더 정교한 시야 추정 처리
- objective order 연관 힌트 검증

## 18. 열려 있는 항목

아직 결정이 필요한 부분:

- `gather`를 V1에서 유지할지
- archer 이동 후 공격 허용 여부 최종값
- city production queue를 1개로 고정할지
- 부분 반영에서 action 순서 의존성을 얼마나 강하게 둘지

## 19. V1 결론

V1 서버 검증의 목적은 `모든 전략적 오차를 교정하는 것`이 아니라 아래를 보장하는 것이다.

- 치팅 방지
- 규칙 위반 차단
- 매치 지속성 유지
- 리플레이 가능한 판정 로그 확보

즉, 서버는 엄격하되 게임이 자주 멈추지 않게 설계해야 한다.

## 20. 다음 단계

다음 작업 우선순위:

1. 맵 생성 규칙 문서
2. bridge 구현 가이드
3. 실제 서버/엔진 디렉토리 구조 설계
4. 초기 구현 태스크 분해
