# Game Rules Spec V1

## 1. 목적

이 문서는 Agent Arena의 V1 게임 규칙 상세 초안이다.

기준:
- 문명 같은 스타일
- 자원 + 영토 + 전쟁 중심
- Fog of War 기반
- 플레이어는 장군처럼 명령
- Codex 같은 개인 에이전트가 실제 전장 행동 수행

이 문서는 현재 체크리스트에서 확정된 방향을 바탕으로, 구현 가능한 수준의 V1 룰 초안으로 정리한 것이다.

## 2. 확정된 방향 요약

- 맵은 `헥스 맵`
- 중간 크기 이상의 전장
- 지형은 `산`, `바다`, `강` 포함
- 자원은 `food`, `wood`, `iron`, `gold`
- 시작은 `수도 1개`
- `settler`로 새 도시 건설 가능
- 유닛은 `scout`, `warrior`, `archer`, `worker`, `settler`
- 행동은 `이동`, `공격`, `생산`, `건설`, `채집`, `점령`, `방어`
- 승리 조건은 `수도 점령 즉시 승리` 또는 `턴 종료 시 점수 승리`
- 지휘 체계는 `turn order + standing orders + objective orders`

## 3. V1 기본 게임 구조

### 3.1 경기 형식
- 1v1
- 턴제 전략 게임
- Fog of War 적용
- 웹에서 시각화
- 각 플레이어는 자기 에이전트를 외부에서 연결

### 3.2 추천 경기 길이
당신 요청에 맞춰 V1 기본값은 아래처럼 둔다.

- 최대 70턴
- 플레이어당 턴 응답 제한 20초
- 전체 경기 시간은 10분보다 더 길어질 수 있음

주의:
- 최대 70턴에 턴당 20초면, 애니메이션을 제외해도 이론상 경기 시간이 길어진다.
- 따라서 `10분 안 종료`는 V1에서 hard limit가 아니라 희망 템포로 보는 편이 맞다.

## 4. 맵 규칙

### 4.1 맵 형식
- 헥스 맵 사용
- 중간 크기 맵 기본값: `가로 24 x 세로 18` 수준
- 추후 small / medium / large 프리셋 확장 가능

### 4.2 지형 종류
- `plains`
- `forest`
- `hill`
- `mountain`
- `river`
- `coast`
- `ocean`

### 4.3 지형 효과 초안
- `plains`: 기본 이동, 기본 식량
- `forest`: 식량/목재 보너스, 이동 불리
- `hill`: 방어 보너스, 시야 보너스 가능
- `mountain`: 통과 불가, 시야 차단
- `river`: 이동 제약 또는 추가 이동 소모
- `coast`: 특정 유닛만 통과
- `ocean`: V1에서는 기본 통과 불가

### 4.4 시야 규칙
- 모든 유닛은 개별 시야 반경을 가진다
- 기본 이동력과 시야는 분리한다
- 미탐색 타일, 탐색 완료 타일, 현재 시야 타일을 구분한다
- 적 유닛은 현재 시야에서 벗어나면 마지막 관측 위치만 추정 정보로 남는다

### 4.5 기본 시야 초안
- `scout`: 시야 4
- `warrior`: 시야 2
- `archer`: 시야 3
- `worker`: 시야 2
- `settler`: 시야 2

## 5. 자원 규칙

### 5.1 자원 종류
- `food`
- `wood`
- `iron`
- `gold`

### 5.2 자원 용도 초안
- `food`: 도시 성장, 유닛 유지, 일부 생산 비용
- `wood`: 건물 및 초기 생산 비용
- `iron`: 군사 유닛 생산과 고급 군사 업그레이드 비용
- `gold`: 범용 비용, 긴급 생산, 유지비, 특수 행동 비용

### 5.3 자원 획득 방식
- 도시 영토 안 자원 타일에서 기본 생산
- `worker`가 개발한 타일은 생산량 증가
- 도시는 매 턴 자원을 수집

### 5.4 자원 밀도 원칙
- 모든 자원이 모든 시작 지점 근처에 다 있지는 않음
- 최소 생존 자원은 확보 가능해야 함
- `iron`과 `gold`는 경쟁 포인트가 되도록 약간 희소하게 배치

## 6. 도시와 영토 규칙

### 6.1 시작 상태
- 각 플레이어는 수도 1개로 시작
- 수도 근처에 기본 자원 접근 가능
- 시작 유닛 구성은 별도 섹션 정의

### 6.2 도시 건설
- `settler`가 새 도시를 건설할 수 있다
- 새 도시는 즉시 기본 영토를 확보한다
- 너무 가까운 도시 배치는 금지한다

### 6.3 영토 규칙
- 도시는 주변 타일을 자동으로 영토화한다
- 영토 안 타일만 자원 생산에 기여한다
- 중립 타일은 점령 전까지 누구 소유도 아님

### 6.4 도시 기능
- 자원 수집
- 유닛 생산
- 영토 확장 거점
- 점수 계산의 핵심 요소

## 7. 유닛 규칙

### 7.1 V1 유닛 목록
- `scout`
- `warrior`
- `archer`
- `worker`
- `settler`

### 7.2 기본 역할
- `scout`: 정찰, 시야 확보, 빠른 이동
- `warrior`: 기본 근접 전투
- `archer`: 원거리 공격
- `worker`: 타일 개발, 자원 효율 상승
- `settler`: 신규 도시 건설

### 7.3 이동력 초안
- `scout`: 이동 4
- `warrior`: 이동 2
- `archer`: 이동 2
- `worker`: 이동 2
- `settler`: 이동 2

참고:
- 체크리스트에 적은 `4칸씩 움직일 수 있게`는 일단 `scout`의 강한 기동성으로 반영하고, 전 유닛 공통 4칸은 V1 밸런스상 과하므로 보수적으로 잡는다.

### 7.4 전투 원칙
- 근접 유닛은 인접 타일 공격
- 원거리 유닛은 사거리 내 공격
- 지형은 전투 보정에 영향
- 도시와 수도는 자체 방어력을 가질 수 있음

## 8. 행동 규칙

### 8.1 허용 행동
- `move`
- `attack`
- `produce`
- `build`
- `gather`
- `capture`
- `fortify`
- `settle_city`
- `end_turn`

### 8.2 행동 제약 원칙
- 각 유닛은 턴당 이동력 한도 내에서 움직인다
- 공격 후 추가 행동 제한 여부는 유닛별로 정한다
- 생산은 도시에서만 가능
- 건설/개발은 `worker`만 가능
- 도시 건설은 `settler`만 가능

## 9. 시작 상태 초안

각 플레이어 시작 구성:
- 수도 1개
- `scout` 1기
- `warrior` 1기
- `worker` 1기
- 초기 자원 소량 지급

의도:
- 초반 정찰
- 기본 방어
- 자원 개발

## 10. 승리 조건

### 10.1 즉시 승리
- 상대 수도 점령 시 즉시 승리

### 10.2 턴 종료 승리
- 최대 70턴 종료 시 점수 계산
- 점수가 더 높은 플레이어 승리

### 10.3 점수 계산 초안
- 도시 수
- 영토 수
- 군사 전력
- 누적 자원 가치

기본 방향:
- 수도 점령이 가장 강한 승리 조건
- 점수 승리는 장기전용 안전장치

## 11. 지휘 규칙

### 11.1 명령 타입
- `turn_order`
- `standing_orders`
- `objective_orders`

### 11.2 V1 추천 제한
- turn order: 턴당 1개
- standing orders: 최대 5개
- objective orders: 활성 최대 3개

이 제한을 추천하는 이유:
- Codex가 너무 많은 목표를 동시에 관리하면 행동이 흐려진다
- 플레이어도 과도한 명령보다 핵심 전략 몇 개를 유지하는 편이 낫다

### 11.3 objective order 예시
- `A 지점을 공격하고 점령하라`
- `북쪽 자원 지대를 확보하라`
- `상대 정찰 유닛을 추적해 제거하라`

### 11.4 우선순위
1. 서버 규칙
2. legal actions
3. turn_order
4. objective_orders
5. standing_orders
6. agent 기본 전략

## 12. Fog of War 세부 규칙

### 12.1 타일 상태
- `unseen`
- `explored`
- `visible`

### 12.2 적 정보 처리
- 시야 내 적은 정확한 현재 정보 표시
- 시야 밖 적은 마지막 관측 기준 추정 정보만 유지
- 일정 턴 이상 보지 못한 적 정보는 더 약하게 표시 가능

## 13. V1 구현 우선순위

### 13.1 먼저 구현할 것
1. 헥스 맵과 타일 모델
2. Fog of War 계산
3. 도시/영토 규칙
4. 기본 유닛 5종
5. 자원 수집
6. 전투
7. objective order 해석 가능한 command context

### 13.2 나중에 붙일 것
- 기술 트리
- 외교
- 해군
- 공성 특화 유닛
- 고급 건물 체계

## 14. 아직 열려 있는 항목

아래는 아직 확정하지 않은 세부값이다.

- 정확한 맵 크기 프리셋
- 각 지형의 구체 이동 비용
- 각 유닛의 체력/공격력/방어력
- 도시 영토 확장 반경
- 점수 계산 가중치
- worker 개발 액션 종류
- archer의 정확한 사거리

## 15. 다음 단계 제안

이 문서를 기준으로 바로 이어갈 작업은 아래 순서가 좋다.

1. `action JSON schema 상세 문서`
2. `objective order 타입 정의`
3. `유닛/자원 수치 밸런스 초안`
4. `맵 생성 규칙 문서`
