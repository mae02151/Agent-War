import { PROTOCOL_VERSION } from "@agent-arena/shared";

function main(): void {
  console.log("[bridge] local agent bridge skeleton ready");
  console.log(`[bridge] protocol version: ${PROTOCOL_VERSION}`);
}

main();

