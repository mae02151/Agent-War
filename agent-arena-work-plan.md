# Agent Arena 작업 계획서

## 1. 프로젝트 개요

### 1.1 프로젝트 한 줄 정의
플레이어가 자기 AI 에이전트를 직접 준비하고, Fog of War 기반 턴제 전략 게임에서 다른 플레이어의 에이전트와 대전시키는 웹 기반 전장 및 대전 플랫폼.

### 1.2 핵심 컨셉
- 유저는 직접 유닛을 조작하지 않는다.
- 유저는 장군처럼 고수준 전략 명령을 내리고, 자기 에이전트가 이를 해석해 실제 전장 행동으로 바꾼다.
- 유저는 자기 에이전트의 프롬프트, 전략 규칙, 전술 성향을 자기 환경에서 조정한다.
- 에이전트는 유저가 개인적으로 사용하는 Codex 같은 모델 또는 자체 AI 스택으로 구성하며, 플랫폼은 모델을 제공하지 않는다.
- 실제 대전은 각자의 에이전트가 서버가 제공하는 게임 상태를 보고 턴마다 행동을 결정해 진행한다.
- 게임의 재미 포인트는 `내가 내린 전략 지시가 전장에서 먹히는 것`, `내 플레이어가 전장을 잘 조작해 승리하는 것`, `그 플레이어를 계속 개선하는 것`에 있다.

### 1.3 목표 사용자 경험
1. 사용자가 자기 에이전트를 만든다.
2. 자기 로컬 환경 또는 개인 서버에서 에이전트를 실행 가능하게 준비한다.
3. 플랫폼에 에이전트를 연결하고 현재 경기의 지휘 명령을 입력한다.
4. 에이전트가 지휘 명령과 observation을 바탕으로 다른 유저의 에이전트와 대전한다.
5. 웹에서 전투를 관전하고 리플레이를 분석한다.
6. 자기 환경에서 프롬프트와 전략을 개선해 더 강한 에이전트로 육성한다.

## 2. 제품 원칙

### 2.1 필수 원칙
- 서버만 전체 맵과 전체 게임 상태를 안다.
- 각 에이전트에는 자기 시야 내 정보만 전달한다.
- 에이전트는 자유 텍스트가 아니라 정해진 액션 스키마로만 행동을 제출한다.
- 모든 경기 결과는 리플레이 가능해야 한다.
- 사용자는 자기 에이전트의 성장과 약점 변화를 체감할 수 있어야 한다.
- 플랫폼은 전장과 판정 환경을 제공하고, 에이전트 자체는 유저가 소유하고 운영한다.
- 사람의 지휘 명령은 게임의 핵심 입력이며, 에이전트는 이를 실제 행동으로 번역하는 역할을 맡는다.

### 2.2 서비스 차별점
- 단순 AI 대전이 아니라 `내가 장군처럼 명령하고, 내가 만든 플레이어가 전장을 지휘해 승리하는 경험`을 핵심으로 가진다.
- 승패뿐 아니라 에이전트 개선, 전략 실험, 약점 분석, 코칭이 메타 루프를 만든다.
- Fog of War를 통해 탐색, 정보전, 추론, 기만 요소가 생긴다.

## 3. 게임 방향성

### 3.1 장르
- 웹 기반 턴제 전략 게임
- 1v1 대전
- Fog of War 적용
- Civilization의 감각을 가져오되, 규칙은 더 작고 실험 가능한 형태로 축소

### 3.2 V1 게임 목표
- 복잡한 4X 전체를 구현하지 않는다.
- `문명-lite 전장` 수준의 구조로 시작한다.
- 에이전트의 판단력과 전략 차이가 드러나는 구조를 우선한다.

### 3.3 V1 게임 규칙 초안
- 2인용 턴제
- 격자형 맵
- 플레이어별 본진 1개
- 자원 2종
- 유닛 3종
- 행동: 이동, 탐색, 생산, 공격, 점령
- 승리 조건: 상대 본진 파괴 또는 턴 종료 시 점수 우세

## 4. 에이전트 연결 및 육성 방식

### 4.1 V1 육성 방식
V1에서는 플랫폼이 모델을 제공하거나 유저별로 학습시키지 않는다. 유저는 자기 환경에서 에이전트를 운영하고, 플랫폼은 표준 프로토콜로 해당 에이전트를 경기 서버에 연결한다.

- 로컬 실행형 에이전트 또는 개인 서버형 에이전트 지원
- HTTP 또는 WebSocket 기반 표준 통신 프로토콜
- 사람의 전략 지시를 에이전트에 전달하는 command layer 제공
- 턴별 직접 지휘 모드와 지속 지휘 모드 지원
- 에이전트 버전 라벨 및 메타데이터 등록
- 테스트 매치와 실전 매치 분리
- 리플레이와 경기 로그를 활용한 외부 개선 루프
- 선택적으로 전략 요약, 노트, 변경 이력만 플랫폼에 저장

### 4.2 왜 V1은 외부 에이전트 연결 방식이어야 하는가
- 유저가 Codex 같은 개인 모델을 그대로 활용할 수 있다.
- 프롬프트와 전략 자산을 플랫폼에 공개하지 않아도 된다.
- 플랫폼의 역할을 전장, 판정, 시각화에 집중시킬 수 있다.
- 임의 코드 실행을 서버에 직접 허용하지 않아도 된다.
- 유저가 자기 개발 환경에서 빠르게 전략 실험과 반복 개선을 할 수 있다.

### 4.3 중장기 육성 로드맵
#### 단계 1. External Prompt Training
- 유저가 자기 환경에서 직접 프롬프트와 전략 규칙을 수정
- 유저가 장군 명령 템플릿과 standing orders를 수정
- 리플레이와 경기 로그를 보고 수동 개선

#### 단계 2. Assisted Coaching
- 시스템이 경기 후 자동 피드백 제공
- 예: 초반 정찰 부족, 자원 투자 과잉, 방어 분산
- 유저는 자기 환경에서 추천안을 반영해 에이전트를 개선

#### 단계 3. Self-Play / Scrimmage Tooling
- 사용자가 자기 에이전트로 스크림, 배치 테스트, self-play를 돌릴 수 있는 도구 제공
- 버전별 성능 비교와 전략 평가 리포트 제공

#### 단계 4. Dataset Export and Training Hooks
- 경기 로그를 학습용 데이터셋으로 내보내는 기능 제공
- 유저가 자기 파이프라인에서 미세 조정 또는 정책 학습을 수행할 수 있게 지원

## 5. 시스템 아키텍처

### 5.1 핵심 컴포넌트
1. Web Frontend
2. Backend API / Game Server
3. Authoritative Game Engine
4. Agent Connector
5. Database
6. Queue / Worker

### 5.2 컴포넌트 역할

#### Web Frontend
- 방 생성 및 대전 신청
- 에이전트 연결 관리
- 지휘 명령 입력
- standing orders 설정
- 전투 관전
- 리플레이 재생
- 전적 및 랭킹 조회

#### Backend API / Game Server
- 유저 인증
- 에이전트 등록 및 연결 정보 관리
- 매치 생성
- 지휘 명령 상태 저장
- 경기 시작 및 결과 저장
- 실시간 이벤트 전송

#### Authoritative Game Engine
- 전체 상태 보유
- 시야 계산
- 자원 처리
- 전투 판정
- 승패 판정
- 액션 검증

#### Agent Connector
- 플레이어별 관측값 생성
- 현재 활성 지휘 명령과 standing orders 포함
- 유저 소유 에이전트 엔드포인트 호출
- 응답 파싱
- timeout 및 invalid action 처리

#### Database
- 유저
- 에이전트
- 에이전트 버전
- 매치
- 턴 로그
- 리플레이 메타데이터
- 랭킹

#### Queue / Worker
- 에이전트 호출
- 비동기 매치 처리
- 경기 후 분석 리포트 생성

### 5.3 권장 아키텍처 흐름
1. 유저가 웹에서 매치를 생성한다.
2. 게임 서버가 초기 맵과 상태를 만든다.
3. 턴마다 엔진이 각 플레이어의 관측값을 생성한다.
4. 플랫폼이 각 플레이어의 현재 지휘 명령과 standing orders를 불러온다.
5. Agent Connector가 observation과 command context를 각 플레이어의 등록된 에이전트 엔드포인트로 전달한다.
6. 서버가 액션을 검증하고 다음 상태를 계산한다.
7. 웹 클라이언트는 WebSocket으로 결과를 받아 시각화한다.
8. 경기 종료 후 로그, 리플레이, 통계를 저장한다.

### 5.4 에이전트 연결 모델
- 각 유저는 자기 에이전트를 로컬 머신 또는 개인 서버에서 실행한다.
- 플랫폼은 유저가 등록한 에이전트 주소 또는 연결 세션을 통해 observation과 command context를 전달한다.
- 에이전트는 정해진 시간 내에 action JSON을 반환해야 한다.
- 플랫폼은 프롬프트 전문이나 모델 내부 상태를 필수로 수집하지 않는다.
- 플랫폼은 규칙 검증, 시간 제한, 리플레이 기록만 책임진다.

### 5.5 지휘 모드

#### Commander Mode
- 사용자가 턴마다 직접 전략 지시를 입력한다.
- 각 턴의 지시는 그 턴 observation과 함께 에이전트로 전달된다.
- 미세 조정된 상황 대응과 즉흥 전략 변경에 적합하다.

#### Standing Orders Mode
- 사용자가 한 번 전략 지시를 입력하면, 이후 여러 턴 동안 계속 유지된다.
- 사용자는 필요할 때만 명령을 갱신한다.
- 에이전트는 기존 standing orders를 바탕으로 계속 전장을 운영한다.
- 장기전 운영과 비동기 플레이에 적합하다.

#### Objective Orders Mode
- 사용자가 `A 지점을 공격하고 점령하라`처럼 목표를 가진 명령을 내린다.
- 해당 명령은 취소, 교체, 완료 처리되기 전까지 계속 유지된다.
- 에이전트는 여러 턴에 걸쳐 경로 확보, 병력 집결, 교전, 점령 시도를 이어서 수행한다.
- 플레이어는 기존 objective order를 유지한 채 다른 전략 지시를 추가할 수 있다.

## 6. 에이전트 입출력 설계

### 6.1 입력 원칙
- 전체 맵을 전달하지 않는다.
- 시야 내 타일, 유닛, 도시, 자원만 전달한다.
- 공개 정보와 비공개 정보를 구분한다.
- legal action 힌트를 포함해 invalid response를 줄인다.
- 전달 형식은 개인 에이전트가 쉽게 구현할 수 있도록 간단하고 안정적인 JSON으로 유지한다.
- 현재 턴 지휘 명령과 지속 지휘 명령을 명시적으로 포함한다.
- 장기 목표형 명령은 고유 ID와 상태를 가진 objective order로 전달한다.

### 6.2 입력 예시
```json
{
  "turn": 14,
  "resources": {
    "food": 12,
    "metal": 7
  },
  "owned_cities": [
    {
      "id": "city_1",
      "hp": 20,
      "position": [3, 5]
    }
  ],
  "owned_units": [
    {
      "id": "unit_12",
      "type": "scout",
      "hp": 6,
      "position": [4, 6],
      "moves_left": 2
    }
  ],
  "visible_tiles": [],
  "visible_enemies": [],
  "legal_actions": [],
  "commander_context": {
    "mode": "objective_orders",
    "turn_order": "이번 턴은 교전보다 정찰을 우선하라",
    "standing_orders": [
      "상대 본진을 찾기 전까지 병력 손실을 최소화한다",
      "본진 주변 3칸 이내는 항상 방어 유닛을 유지한다"
    ],
    "objective_orders": [
      {
        "order_id": "ord_17",
        "type": "capture_zone",
        "target": {
          "zone_id": "A",
          "position": [12, 4]
        },
        "status": "active",
        "issued_at_turn": 10,
        "cancel_policy": "until_cancelled"
      }
    ]
  }
}
```

### 6.3 출력 예시
```json
{
  "actions": [
    {
      "type": "move",
      "unit_id": "unit_12",
      "to": [5, 6]
    },
    {
      "type": "produce",
      "city_id": "city_1",
      "unit_type": "soldier"
    }
  ]
}
```

### 6.4 검증 규칙
- JSON schema 불일치 시 invalid action 처리
- 없는 유닛 조작 시 무효 처리
- 시야 밖 정보를 가정한 행동은 서버 기준으로 검증
- timeout 발생 시 기본 행동 또는 패스 처리
- objective order는 장기적으로 유지되지만, 실제 action은 매 턴 서버 기준으로 다시 검증한다

## 7. 웹 기능 범위

### 7.1 V1 필수 기능
- 회원 가입 및 로그인
- 에이전트 등록
- 에이전트 연결 상태 확인
- Commander Mode 지휘 입력
- Standing Orders 저장 및 수정
- Objective Orders 생성, 취소, 우선순위 변경
- 테스트 매치 실행
- 1v1 친선전
- 전투 관전 화면
- 리플레이 재생
- 전적 및 랭킹 조회
- 에이전트 버전 라벨 및 메모 관리

### 7.2 관전 화면 핵심 요소
- 맵 렌더링
- Fog of War 표현
- 유닛 이동 및 전투 애니메이션
- 턴 로그
- 현재 지휘 명령 표시
- standing orders 표시
- 활성 objective orders 표시
- 현재 자원 및 주요 상태 표시
- 경기 종료 요약

### 7.3 에이전트 관리 화면 핵심 요소
- 에이전트 이름과 소개
- 연결 방식과 엔드포인트 상태
- 사용 중인 버전 라벨
- 전략 요약 또는 공개 가능한 설명
- 기본 지휘 템플릿
- 현재 standing orders
- 현재 objective orders
- 최근 heartbeat 또는 마지막 응답 시각
- 버전 메모 및 교체 기록
- 최근 경기 결과 및 승률

## 8. 개발 단계 계획

### Phase 0. 기획 확정
기간: 1주

목표:
- 게임 규칙 문서 작성
- 에이전트 입력/출력 스키마 확정
- commander command model 확정
- MVP 범위 확정
- 기술 스택 결정

산출물:
- 게임 규칙 명세서
- API / 스키마 초안
- 아키텍처 다이어그램

### Phase 1. 로컬 게임 엔진 MVP
기간: 2주

목표:
- CLI 기반 게임 엔진 구현
- 맵 상태 모델 구현
- Fog of War 계산 구현
- 더미 봇 2종 구현
- 리플레이 로그 형식 정의

산출물:
- 로컬 시뮬레이터
- 샘플 경기 로그

### Phase 2. Agent Connector 통합
기간: 2주

목표:
- 외부 에이전트 연결 인터페이스 구현
- 플레이어별 observation 생성
- command context 전달
- objective order lifecycle 전달
- 액션 파싱 및 검증
- timeout / invalid action 처리
- 자동 대전 실행

산출물:
- 에이전트 대전 가능 백엔드 MVP

### Phase 3. 웹 시각화 MVP
기간: 2주

목표:
- 웹에서 경기 관전 가능
- 지휘 명령 상태 표시
- objective order 상태 표시
- 턴 애니메이션 구현
- 리플레이 뷰어 구현
- 경기 결과 페이지 구현

산출물:
- 관전 가능한 웹 UI

### Phase 4. 에이전트 연결 및 운영 UI
기간: 2주

목표:
- 에이전트 등록 화면
- 연결 상태 및 health check 표시
- 버전 라벨 저장
- 버전 메모 관리
- standing orders 관리
- objective orders 관리
- 테스트 매치 기능
- 에이전트 상세 페이지

산출물:
- 유저가 자기 에이전트를 연결하고 실험할 수 있는 UI

### Phase 5. 멀티유저 대전
기간: 2주

목표:
- 유저 계정 연동
- 친구 대전
- 비동기 래더전
- Elo/MMR 반영
- 전적 및 랭킹 저장

산출물:
- 실제 서비스 가능한 기본 멀티플레이 구조

### Phase 6. 분석 및 코칭
기간: 2주

목표:
- 경기 리포트 생성
- 약점 태그 분석
- 개선 제안 자동 생성
- 버전별 성능 비교

산출물:
- 에이전트 육성 경험을 강화하는 분석 기능

## 9. 기술 스택 제안

### 9.1 권장 스택
- Frontend: React + TypeScript + PixiJS
- Backend API: FastAPI
- Realtime: WebSocket
- Engine: Python
- Database: PostgreSQL
- Queue: Redis
- Worker: Python worker

### 9.2 선택 이유
- Python은 게임 엔진과 에이전트 실행 로직 구현 속도가 빠르다.
- React + PixiJS는 전략 맵 시각화에 적합하다.
- WebSocket은 턴 진행과 애니메이션 동기화에 적합하다.
- PostgreSQL은 매치, 리플레이, 버전 이력 관리에 안정적이다.

## 10. 데이터 모델 초안

### 10.1 주요 엔티티
- users
- agents
- agent_versions
- matches
- match_turns
- replays
- rankings
- training_reports

### 10.2 저장해야 하는 핵심 정보

#### agents
- 소유자
- 에이전트 이름
- 연결 방식
- 엔드포인트 정보
- 현재 활성 버전

#### agent_versions
- 버전 라벨
- 전략 요약
- 공개 가능한 메모
- 기본 지휘 템플릿
- 생성 시각
- 연결 설정 스냅샷
- 성능 통계

#### commander_profiles
- 소유자
- 모드 종류
- 현재 standing orders
- 현재 objective orders
- 최근 턴 지휘 기록
- 수정 시각

#### matches
- 참가 에이전트
- 맵 시드
- 승자
- 종료 사유
- 시작/종료 시각

#### match_turns
- 턴 번호
- 플레이어별 observation 요약
- 제출 액션
- 판정 결과
- 상태 변화 로그

## 11. 주요 리스크와 대응

### 11.1 범위 폭발
리스크:
- 문명형 시스템은 자원, 생산, 기술, 시야, 전투 등으로 빠르게 커진다.

대응:
- 본진 1개, 자원 2종, 유닛 3종으로 시작
- 기술 트리와 외교는 V1 제외

### 11.2 에이전트 응답 지연
리스크:
- 턴마다 외부 에이전트 응답을 기다려야 하므로 경기 진행이 느려질 수 있다.

대응:
- 작은 맵과 짧은 턴 제한으로 시작
- observation을 요약해 전달
- timeout과 기본 fallback 행동을 제공

### 11.3 공정성 문제
리스크:
- 유저별 모델 차이가 지나치게 크면 게임성이 무너질 수 있다.

대응:
- 모든 플레이어에게 동일한 observation 형식과 동일한 턴 제한을 적용
- 행동 스키마와 서버 판정 로직을 완전히 통일
- 허용 모델 범위를 아예 제한할지, 오픈 경쟁으로 둘지는 운영 정책으로 분리

### 11.4 이상 응답 및 규칙 위반
리스크:
- 에이전트가 잘못된 JSON 또는 불법 행동을 제출할 수 있다.

대응:
- 엄격한 schema 검증
- invalid action 패널티
- fallback 행동 정의

### 11.5 보안 및 격리
리스크:
- 외부 에이전트 엔드포인트 호출에는 인증, 위조 응답, 과도한 지연 문제가 있다.

대응:
- 플랫폼 서버에서는 유저 코드를 직접 실행하지 않는다.
- 서명된 요청, timeout, 재시도 제한, rate limit, schema 검증을 적용한다.

## 12. 성공 지표

### 12.1 제품 지표
- 주간 활성 유저 수
- 에이전트 생성 수
- 에이전트 버전 수정 횟수
- 경기 수
- 리플레이 조회 수

### 12.2 핵심 재미 지표
- 유저가 자기 에이전트를 반복적으로 개선하는가
- 유저가 자기 에이전트가 전장을 더 잘 운영한다고 체감하는가
- 유저가 지휘 명령을 전략적으로 활용한다고 느끼는가
- standing orders만으로도 의미 있는 장기 운영이 가능한가
- objective order가 여러 턴에 걸쳐 일관되게 수행된다고 느끼는가
- 버전 개선 후 성능 향상을 체감하는가
- 리플레이와 코칭 기능 사용률이 높은가
- 랭킹전 참여율이 유지되는가

## 13. 우선순위 결론

가장 먼저 만들어야 할 것은 아래 3가지다.

1. 작은 범위의 Fog of War 전략 엔진
2. 에이전트 입력/출력 프로토콜
3. 지휘 명령 모델
4. 리플레이 가능한 웹 관전 화면

이 4개가 먼저 완성되어야 이후의 에이전트 연결 관리, 랭킹, 코칭 기능이 의미 있게 붙는다.

## 14. 바로 다음 액션

다음 작업 순서는 아래를 권장한다.

1. 게임 규칙 1페이지 명세 작성
2. 턴 단위 observation / action JSON schema 정의
3. commander / standing orders / objective orders schema 정의
4. CLI 시뮬레이터 구현
5. 더미 봇 2종으로 자동 대전 테스트
6. 웹 리플레이어 구현
7. 에이전트 연결 UI 구현

---

이 문서는 프로젝트 착수용 상위 계획서이며, 다음 문서로 아래 항목을 이어서 작성하는 것을 권장한다.

- MVP 요구사항 문서
- 게임 규칙 상세 명세
- DB 스키마 초안
- API 명세
- 에이전트 연결 프로토콜 문서
- commander mode 상세 명세
