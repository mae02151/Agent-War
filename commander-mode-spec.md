# Commander Mode 상세 명세

## 1. 목적

이 문서는 Agent Arena에서 사람이 장군처럼 전략 명령을 내리고, 개인 에이전트가 그 명령을 해석해 전장을 운영하는 방식의 상세 규칙을 정의한다.

핵심 목표:
- 플레이어가 직접 미시 조작을 하지 않아도 전장 개입감을 느낄 수 있어야 한다.
- Codex 같은 개인 에이전트가 사람의 의도를 여러 턴에 걸쳐 일관되게 수행할 수 있어야 한다.
- 턴별 즉시 지시와 장기 목표 지시를 함께 사용할 수 있어야 한다.

## 2. 지휘 모델 개요

지휘 입력은 세 층으로 나뉜다.

1. `turn_order`
- 이번 턴에만 적용되는 직접 지시

2. `standing_orders`
- 취소 또는 수정 전까지 유지되는 일반 방침

3. `objective_orders`
- 특정 목표를 달성할 때까지 지속되는 장기 명령

에이전트는 observation과 함께 이 세 가지 지휘 입력을 받아 실제 action으로 변환한다.

## 3. 세 가지 지휘 타입

### 3.1 Turn Order

예시:
- `이번 턴은 정찰 우선, 교전은 피하라`
- `북쪽 병력을 후퇴시켜라`

특징:
- 기본적으로 1턴만 유효
- 가장 높은 우선순위를 가짐
- 전황 급변 대응용

### 3.2 Standing Orders

예시:
- `상대 본진을 찾기 전까지 무리한 교전을 피하라`
- `본진 주변에는 항상 최소 1기 방어 유닛을 유지하라`

특징:
- 여러 턴 유지
- 플레이 스타일과 운영 방침을 정의
- 취소 또는 수정 전까지 계속 적용

### 3.3 Objective Orders

예시:
- `A 지점을 공격하고 점령하라`
- `남쪽 자원 지대를 확보하라`
- `적 정찰 유닛을 추적해 제거하라`

특징:
- 목표 달성 전까지 지속
- 여러 턴에 걸쳐 수행
- 취소, 교체, 완료 처리 전까지 활성 상태 유지
- 실제 행동은 상황에 따라 에이전트가 유연하게 구성

## 4. 핵심 사용자 경험

플레이어는 다음처럼 게임한다.

1. 장기 목표를 건다.
2. 전체 운영 방침을 설정한다.
3. 필요한 턴에만 직접 지시를 덧붙인다.
4. Codex가 여러 턴 동안 전장을 대신 운영한다.
5. 플레이어는 필요할 때 명령을 수정하거나 취소한다.

즉, 플레이어는 `전략 의도`를 설계하고, 에이전트는 `전술 실행`을 담당한다.

## 5. Objective Order의 의미

Objective order는 단순 메모가 아니라 `활성 목표`다.

예를 들어 플레이어가:

`A 지점을 공격하고 점령하라`

라고 지시하면, 에이전트는 이 명령을 다음처럼 장기 과업으로 해석할 수 있다.

- A 지점까지 안전한 경로 탐색
- 병력 집결
- 적 유닛 제거
- 점령 가능한 위치 확보
- 점령 시도
- 점령 이후 방어 유지

중요한 점:
- 이 명령은 한 턴만 수행하고 끝나는 것이 아니다.
- 에이전트는 취소 전까지 계속 A 지점 점령을 시도해야 한다.
- 플레이어는 동시에 다른 전략 지시를 추가할 수 있다.

## 6. 명령 수명주기

### 6.1 Turn Order 수명주기
- 생성
- 해당 턴 처리
- 자동 만료

### 6.2 Standing Orders 수명주기
- 생성
- 유지
- 수정 또는 삭제

### 6.3 Objective Orders 수명주기
- 생성
- 활성
- 일시 정지
- 완료
- 취소
- 실패

## 7. Objective Order 상태 정의

### `active`
- 현재 수행 중

### `paused`
- 유지되지만 일시적으로 우선순위에서 제외

### `completed`
- 목표 달성 완료

### `cancelled`
- 플레이어가 명시적으로 취소

### `failed`
- 현재 경기 상태상 수행 불가 또는 실패로 판정

## 8. 우선순위 규칙

기본 우선순위:

1. 서버 규칙
2. legal actions
3. turn_order
4. objective_orders
5. standing_orders
6. 에이전트 내부 기본 전략

의미:
- 직접 지시는 즉시 반영해야 한다.
- objective order는 장기 목표를 유지한다.
- standing orders는 전체 스타일과 성향을 정한다.

## 9. 명령 충돌 규칙

예시:
- standing order: `무리한 교전을 피하라`
- objective order: `A 지점을 공격하고 점령하라`
- turn order: `이번 턴은 후퇴하라`

해석:
- 이번 턴에는 후퇴
- 이후에는 다시 A 지점 점령 시도 재개
- 단, 점령 과정에서도 불필요한 손실은 피하도록 행동

즉, 충돌은 제거보다 `계층적 해석`으로 푸는 것이 원칙이다.

## 10. 명령 형식 초안

### 10.1 Turn Order

```json
{
  "text": "이번 턴은 정찰을 우선하고 교전은 피하라"
}
```

### 10.2 Standing Orders

```json
[
  "상대 본진 위치를 찾기 전까지는 확장보다 탐색을 우선한다",
  "본진 주변에는 항상 최소 1기 방어 유닛을 남긴다"
]
```

### 10.3 Objective Order

```json
{
  "order_id": "ord_17",
  "type": "capture_zone",
  "summary": "A 지점을 공격하고 점령하라",
  "target": {
    "zone_id": "A",
    "position": [12, 4]
  },
  "priority": "high",
  "status": "active",
  "cancel_policy": "until_cancelled"
}
```

## 11. Objective Order 타입 예시

- `capture_zone`
- `defend_zone`
- `gather_resource_area`
- `hunt_unit`
- `escort_unit`
- `expand_to_region`

V1에서는 너무 많게 가지 않고 3~5개 정도로 제한하는 것이 좋다.

## 12. UI/UX 요구사항

### 12.1 플레이어 입력 화면
- turn order 입력 박스
- standing orders 목록
- objective orders 목록
- objective order 추가 버튼
- objective order 취소 버튼
- objective order 일시정지 버튼

### 12.2 관전 화면
- 현재 turn order 표시
- 활성 standing orders 표시
- 활성 objective orders 표시
- 각 objective order의 상태 표시

## 13. 플레이 흐름 예시

1. 플레이어가 objective order 생성
`A 지점을 공격하고 점령하라`

2. standing orders 유지
`상대 본진을 찾기 전까지 무리한 교전을 피하라`

3. 특정 턴에 turn order 추가
`이번 턴은 남쪽 병력을 잠시 후퇴시켜라`

4. Codex는 이번 턴 후퇴를 수행

5. 다음 턴부터는 다시 A 지점 점령 objective를 계속 수행

6. 플레이어가 필요하면 objective order 취소 또는 교체

## 14. 에이전트 해석 원칙

에이전트는 아래 원칙으로 명령을 해석해야 한다.

- turn order는 즉시 반영
- objective order는 장기 목표로 유지
- standing orders는 일반 정책으로 유지
- 직접 실행 불가능한 지시는 observation 기준 가장 가까운 유효 행동으로 변환
- 한 턴에 목표를 완료하지 못해도 목표를 버리지 않음

## 15. 서버 책임

플랫폼 서버는 아래를 책임진다.

- 현재 활성 명령 상태 저장
- 각 턴 observation에 command context 포함
- objective order 상태 관리
- 취소/완료/실패 이벤트 기록
- 리플레이에 명령 변경 이력 저장

## 16. V1 결론

V1에서 가장 중요한 것은 플레이어가 다음 감각을 얻는 것이다.

- 내가 명령하면 전장이 그렇게 움직인다
- 내 명령은 한 턴짜리도 되고 장기 전략도 된다
- Codex는 단순 자동봇이 아니라 내 참모이자 지휘 대리인처럼 행동한다

이 감각이 살아야 이 게임의 차별점이 생긴다.
